pipeline {
    agent {
        docker {
            image 'node:18-alpine'  // Versión ligera con Node.js y Shell
            args '-v /var/run/docker.sock:/var/run/docker.sock'
        }
    }
    stages {
        stage('Build') {
            steps {
                echo '🔨 Building SICEI...'
                sh 'npm install'  // Ahora funcionará
            }
        }
        stage('Deploy') {
            steps {
                echo '🚀 Deploying SICEI...'
                sh 'docker build -t sicei-app .'
            }
        }
    }
}