pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Instalando dependencias y construyendo imagen Docker...'
                sh 'npm install'
                sh 'docker build -t sicei-app .'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Desplegando contenedor...'
                sh 'docker run -d -p 3000:3000 sicei-app'
            }
        }
    }
}
